rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Il sito non usa più Firestore: nessuna lettura/scrittura è consentita.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
